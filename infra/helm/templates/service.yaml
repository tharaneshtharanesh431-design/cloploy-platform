apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloploy-app.fullname" . }}
  labels:
    {{- include "cloploy-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "cloploy-app.selectorLabels" . | nindent 4 }}
